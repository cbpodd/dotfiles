# Java Default set to 1.8
export JAVA_HOME=$(/usr/libexec/java_home -v 1.8)

source /Users/cbpodd/Library/Preferences/org.dystroy.broot/launcher/bash/br

# Path
export PATH="/Users/cbpodd/bin/context/tex/texmf-osx-64/bin:/Users/cbpodd/sbin:/Users/cbpodd/bin:/usr/local/bin:/usr/local/sbin:/Users/cbpodd/.bin/apache-maven-3.6.3/bin:/Users/cbpodd/.yarn/bin:/Users/cbpodd/.config/yarn/global/node_modules/.bin:/usr/bin:/bin:/usr/sbin:/sbin:/Library/TeX/texbin"
fpath+=~/.config

ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE='fg=#B3B3B3'
source ~/.config/antigen.zsh
antigen init ~/.config/antigen/antigenrc
export EDITOR=vim

# Custom Commands

# Merges branch $1 into branch $2
function gitMerge() {
  	git checkout $1
	git pull
	git checkout $2
	git merge $1
	git push
}

function texc() {
    file=$1
    CORE=${file%\.*}
    pdflatex "${file}"
    open -a Preview "${CORE}.pdf"
}

RIGHT_MON_ID='E0CF5B67-FDDF-13D4-8C8D-64D7900F18A8'
LEFT_MON_ID='6652BC2D-F818-E3CE-F64D-C9E890B19458'
INTERNAL_SCREEN_ID='1B6E5E4F-F885-D5D9-12AC-423EA9E16E72'

INTERNAL_MODE='res:1680x1050 color_depth:8 scaling:on'
MON_MODE='res:1680x1050 hz:59 color_depth:4 scaling:off'
SINGLE_MODE='res:1280x800 color_depth:8 scaling:on'
NO_ROTATE='degree:0'

function normalDisplays() {
    displayplacer\
        "id:$INTERNAL_SCREEN_ID $INTERNAL_MODE origin:(1680,0) degree:0" \
        "id:$RIGHT_MON_ID $MON_MODE $NO_ROTATE origin:(0,0)" \
        "id:$LEFT_MON_ID $MON_MODE $NO_ROTATE origin:(-1680,0)"
}

function mirrorDisplays() {
    displayplacer "id:$RIGHT_MON_ID+$LEFT_MON_ID+$INTERNAL_SCREEN_ID $MON_MODE $NO_ROTATE origin:(0,0)"
}

function singleDisplay() {
    displayplacer "id:$INTERNAL_SCREEN_ID $SINGLE_MODE $NO_ROTATE origin:(0,0)"
}

function brewInstall() {
    brew update
    brew upgrade
    brew install $1

    if [-e ~/.config/brew/Brewfile ]
    then
        rm ~/.config/brew/Brewfile
    fi

    brew bundle dump --file=~/.config/brew/Brewfile
}

# Aliases

# Overwrites of existing commands for better defaults
alias mv="mv -i"             # -i prompts before overwrite
alias mkdir="mkdir -p"       # -p makes parents dirs as needed
alias cp="cp -r"             # -r copies directories recursively
alias make="make -j$(nproc)" # -j$(nproc) uses nproc concurrent jobs   
alias cat="cat -n"           # -n displays line numbers

# Common Flags
alias la="ls -A"
alias ll="ls -lh"
alias lla="ll -A"

# Git
alias g="git"
alias ga="g add ."
alias gc="g commit -S -m"
alias gs="g status"
alias gpl="g pull"
alias gph="g push"
alias gb="g branch"
alias gch="g checkout"
alias gf="g fetch"
alias gr="g remote"
alias grao="gr add origin"
alias ggraph="git log --oneline --graph --decorate --all"

# Vim
alias v="vim"

# VirtualBox Command Line
alias vbox="VBoxManage"
alias vbsa='vbox startvm "Ubuntu Server" --type headless'
alias vbso='vbox controlvm "Ubuntu Server" poweroff --type headless'
alias vboxc='vbsa && ssh vbox && vbso'

# Shpotify - Spotify Command Line
alias s="spotify"
alias sn="s next"
alias spv="s prev"
alias sp="s pause"
alias spy="s play"
alias sq="s quit"
alias ss="s status"

# 3llo - Trello
alias trello="3llo"

# Common Vim Commands
alias :e=vim
alias :q=exit

# Make
alias m="make"
alias mc="m clean"

# My own displayplacer commands
alias nd="normalDisplays"
alias md="mirrorDisplays"
alias sd="singleDisplay"

# Brew
alias bi="brewInstall"

# Others
alias rq='sed -E "s/\"//g"'
